{
  "name": "kanbanish-on-azure",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "npm run build-client && npm run build-server",
    "build-client": "cd client/kanban-react && npm run build  && cd ../..",
    "build-server": "echo 0",
    "postinstall": "npm run build",
    "start": "node server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.7.12",
    "swagger-ui-express": "^4.1.2"
  }
}
